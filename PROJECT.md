# このリポジトリの設定

このファイルだけが導入先固有の設定を所有する。製品の仕様本文は製品側の設計資料へ置く。
他プロジェクトへ導入するときは[設定テンプレート](docs/harness/templates/project.md)を使って置き換える。

| 項目 | 現在の設定 |
| --- | --- |
| 対象 | Astra向け開発運用一式の保守 |
| 製品種別 | 開発用ドキュメントとローカル検査ツール |
| 言語 | 説明・文書は日本語、スクリプトはJavaScript ES Modules・Python |
| 実行環境 | Node.js 22以降、Git、Python 3.10以降。3OSのCI確認済み。Codexデスクトップの実接続はWindows確認済み |
| 基点・統合先 | main |
| 保護ブランチ | main、develop |
| 作業ブランチ | codex/<type>-<short-name> |
| Git完了の権限 | 明示依頼された範囲でcommit・push・PR・merge。通常の実装依頼では最終検証まで |
| 初期作成 | 新規の独立Git履歴を作業ブランチで作成し、初回内容の確定後にmainへ名称変更できる |
| リポジトリ公開範囲 | private。public化は別途明示承認が必要 |
| 公開候補 | 監査済みスナップショットから独立履歴を作り、旧PR・キャッシュを持ち込まない |
| PR作成の条件 | GitHubの作者設定と非公開検証repoの仮マージidentityを確認するまで作成しない。承認済みheadのfast-forwardは利用可 |
| ライセンス | MIT |
| 新しい依存 | 現在は外部npm依存なし。追加は事前に理由と影響を確認 |
| 文書チェック | node tools/harness/check.mjs |
| ツールのテスト | npm test。HTML回答受付は npm run test:feedback |
| 配布内容チェック | node tools/harness/scan.mjs |
| 証跡 | .local/（非配布）、UI画像はscreenshots/（非配布） |

## 保護する契約

- 特定作品の内容、利用者の実データ、認証情報、個人用パス、過去の製品Git履歴を持ち込まない。
- モデル・推論設定・システム共通スキルを自動変更しない。
- プロジェクト別の言語、画面サイズ、公開先、ブランチ、コマンドをworkflowへ固定しない。
- 検査ツールは読み取り専用。失敗を直すためにファイル削除やGitの履歴改変を自動実行しない。
- 設定の未確定は影響する操作だけの判断点にし、読み取りや承認済みローカル作業を一律停止しない。

## このリポジトリでは対象外

製品サーバー、製品のセーブ領域、認証基盤、ストア公開、デプロイ環境はない。
外部相談のクライアントは同梱するが、利用契約・APIキー・実際の有料送信は導入先で設定・確認する。
ハーネスの検証を製品のUI・API・負荷試験の成功と報告しない。
