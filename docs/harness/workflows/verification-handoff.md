# 親だけでは検証できない場合

利用できるツールで確認できる範囲を先に終える。
物理端末、ログイン、利用契約、ユーザーの美的判断など、実際に必要な部分だけ引き継ぐ。

結果の説明には、確認済み、未確認、その理由、実行手順、期待結果を含める。
抽象的に「確認してください」で終えず、どの画面で何を操作すれば何が起きるかを書く。
結果を受けた後に継続する作業も示す。

tool拒否の場合は、拒否された操作と理由を分けて説明する。
同じ操作を別ツールや権限変更で回避しない。未確認を成功扱いしない。
不要な環境再設定やサーバー停止をユーザーへ要求しない。
