# データの保護と不具合の報告

このハーネスは、本人が管理するPCとGitプロジェクトで使うローカル開発ツールです。
共有サーバーで複数利用者を隔離する製品ではありません。公開Webサーバーとして起動しないでください。

## 何が保存・送信されるか

| 操作 | 保存先・送信先 |
| --- | --- |
| HTMLの途中保存 | そのブラウザのlocalStorage。提出まではPCの回答ファイルへ送らない |
| 回答の提出 | 指定したローカル専用フォルダ。Codexへは提出IDと保存先を通知 |
| Gemini相談 | --send時だけ、指定した相談文・画像をGoogleへ送信 |
| Gemini接続確認 | モデル情報の取得だけ。相談文・画像・生成要求を送らない |
| Pro資料作成 | .localへ保存。作成ツール単独では外部へ送信しない |
| Pro相談 | 利用者が依頼した範囲の資料を、ログイン中のChatGPTへ添付・送信 |

HTML受付はloopbackが既定です。接続キー、固定宛先、Host・Origin検査、配信ファイルの許可一覧を使います。
接続URL自体を秘密として扱ってください。LANのHTTPは暗号化されないため、信頼できないネットワークでは共有しません。
Mac/Linuxの専用保存フォルダは0700、回答・接続情報・通知DB・相談結果は0600にします。
WindowsのAPIキーは現在のユーザーのDPAPIで暗号化します。他の保存物には保存先のWindows ACLが適用されます。
共有Windows端末では本人専用のフォルダを使い、導入時に他ユーザーのアクセス権を確認します。
同じOSアカウント、管理者、ブラウザ拡張、バックアップ先からのアクセスを防ぐ仕組みではありません。

## 問題を見つけたら

公開IssueへAPIキー、私用メール、回答ファイル、実データ、接続URLを貼らないでください。
実害のない合成データで、版・OS・操作・期待結果と実際の結果をまとめます。
機密を含む報告は、対象repoのSecurityタブで非公開報告が利用可能ならその経路を使い、
利用不可なら内容を公開せず、管理者と非公開の連絡手段を確認してください。
キー漏えい時は所有者が無効化・再発行を判断します。Git履歴の削除だけでは無効化されません。

自動scanは補助です。未登録の秘密や、自然文・画像に含まれる個人情報の完全検出は保証しません。
[配布前の確認](docs/harness/publication.md)と人による内容レビューを併用します。
