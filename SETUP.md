# 「セットアップ」で導入する

導入先をCodexで開き、このファイルを読み込ませて「このプロジェクトにセットアップして」と伝える。
導入先が明らかなら再質問せず、[導入手順](docs/harness/setup.md)を実行する。
このハーネス自身の保守フォルダを、別製品の導入先と取り違えない。

親が環境確認、必要ファイルの配置、既存ルールとの統合、PROJECT.mdの記入、検査、HTML接続確認まで担当する。
利用者に一連のコマンドを手作業で実行させることを通常手順にしない。
APIキーの発行・秘密入力、アカウントのログイン、利用契約・課金の選択だけは本人に渡す。
未設定の機能があっても、独立した機能の導入を完了する。

| 導入後の言い方 | 動作 |
| --- | --- |
| ぬ / 次なに | タスク一覧のHTMLを開く。選択・提出から同じタスクで続ける |
| ハンドオーバー | 記録・教訓・検証・承認済みGit統合と後片付け |
| LESSONに残して | 再発条件と対策を記録し、次の関連作業で読む |
| Geminiレスキュー | 実画面の見た目・デザイン、または創作の相談 |
| GPT-6 Proに相談 | 範囲を絞った資料をChatGPTへ送り、別視点の回答を得る |

これはエージェントが実行する導入手順。OSの権限、ログイン、外部サービスの利用権を自動取得するものではない。
