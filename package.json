{
  "name": "kawaken-style-astra-dev-workflow-kit",
  "version": "0.4.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "description": "Kawaken style Astra Dev Workflow Kit: Japanese workflows, skills, templates, and local tools for game, app, and web-service development with Astra in Codex.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "docs:check": "node tools/harness/check.mjs",
    "test": "node --test tests/harness.test.mjs",
    "test:feedback": "node --test tests/feedback/bridge.test.mjs tests/feedback/client.test.mjs && python -X utf8 -m unittest discover -s tests/feedback -p test_*.py",
    "harness:scan": "node tools/harness/scan.mjs",
    "test:setup": "python -X utf8 -m unittest discover -s tests/setup -p test_*.py",
    "harness:doctor": "python -X utf8 tools/harness/setup.py doctor"
  }
}
